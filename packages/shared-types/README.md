# @graphscope/shared-types

Domain DTOs shared across API, DB, and (optionally) clients.

**Allowed dependents:** `@graphscope/db`, `@graphscope/api`, `@graphscope/schema-tools`, `@graphscope/ui`, `@graphscope/web`.

**Public API:** `src/index.ts` only.
